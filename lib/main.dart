import 'package:flutter/material.dart';

void main() {
  runApp(const shopspheir());
}

class shopspheir extends StatelessWidget {
  const shopspheir({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
